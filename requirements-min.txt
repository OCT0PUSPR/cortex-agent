# Minimal dependencies for CI: lint + type-check + run the FULL test suite with
# the MockLLM backend only. No torch, no Redis, no API keys, no network needed.
# Versions are pinned at/above the patched releases flagged by pip-audit.

# --- test + quality tooling ---
# pytest 9.0.3 fixes GHSA-6w46-j5rx-g56g but requires Python >=3.10; on 3.9 we
# use the latest 3.9-compatible release (the advisory affects test collection,
# not production code paths).
pytest>=9.0.3; python_version >= "3.10"
pytest>=8.4.2; python_version < "3.10"
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.4.0
mypy>=1.8.0
bandit>=1.7.0
pip-audit>=2.7.0

# --- app stack exercised by the tests ---
httpx>=0.27.0
rich>=13.7.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.2.2          # GHSA-mf9w-mj56-hr94 fixed in 1.2.2
email-validator>=2.1.0
fastapi>=0.110.0
# starlette 1.3.1 fixes PYSEC-2026-161 / GHSA-* but requires Python >=3.10.
starlette>=1.3.1; python_version >= "3.10"
starlette>=0.40.0; python_version < "3.10"
uvicorn>=0.29.0
sse-starlette>=2.1.0
python-multipart>=0.0.9
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
greenlet>=3.0.0
alembic>=1.13.0
structlog>=24.1.0
prometheus-client>=0.20.0
tenacity>=8.2.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
