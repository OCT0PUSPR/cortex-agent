# Core runtime dependencies for cortex-agent.
# LLM
anthropic>=0.40.0
# HTTP / networking
httpx>=0.27.0
# Config
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
# API server + SSE streaming
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sse-starlette>=2.1.0
# CLI
rich>=13.7.0

# --- Optional / heavy ML extras (uncomment or use requirements-ml.txt) ------
# Vector recall (sentence-transformers / transformers / torch) is optional;
# the memory store falls back to keyword recall when these are absent.
# sentence-transformers>=2.7.0
# transformers>=4.40.0
# torch>=2.2.0
