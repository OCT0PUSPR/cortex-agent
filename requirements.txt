# Core runtime dependencies for cortex-agent.
# LLM
anthropic>=0.40.0
# HTTP / networking
httpx>=0.27.0
# Config
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.2.2          # GHSA-mf9w-mj56-hr94 fixed in 1.2.2
email-validator>=2.1.0
# API server + SSE streaming
fastapi>=0.110.0
# starlette 1.3.1 fixes PYSEC-2026-161 / GHSA-* but requires Python >=3.10
# (Docker targets 3.11); on 3.9 the latest compatible release is used.
starlette>=1.3.1; python_version >= "3.10"
starlette>=0.40.0; python_version < "3.10"
uvicorn[standard]>=0.29.0
sse-starlette>=2.1.0
python-multipart>=0.0.9
# CLI
rich>=13.7.0
# Persistence (SQLAlchemy 2.0 async + migrations)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
greenlet>=3.0.0              # required by SQLAlchemy async
alembic>=1.13.0
# Observability (structured logs + Prometheus metrics)
structlog>=24.1.0
prometheus-client>=0.20.0
# Reliability (retries / backoff for the resilient backend)
tenacity>=8.2.0
# Security (JWT auth + bcrypt password hashing — used directly, not via passlib)
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
# Optional queue worker (Redis-backed); harmless if Redis is absent.
arq>=0.25.0
redis>=5.0.0

# --- Optional / heavy ML extras (TinyBrain: see requirements-train.txt) ------
# Vector recall (sentence-transformers / transformers / torch) is optional;
# the memory store falls back to keyword recall when these are absent.
# sentence-transformers>=2.7.0
# transformers>=4.40.0
# torch>=2.2.0
